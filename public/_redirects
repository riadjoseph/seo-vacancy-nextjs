# Serve assets with proper MIME types
# Removed as we let Netlify handle this part
# /assets/*.js   /assets/:splat   200!   Content-Type=application/javascript
# /assets/*.css  /assets/:splat   200!   Content-Type=text/css

# This rule tells Netlify to handle all routes that don't match static files or the other rules
# by serving the SPA's index.html. This is where the client-side router takes over.
/*  /index.html  200

# This rule must be after the rule above. It will handle the non existant pages
/* 404
