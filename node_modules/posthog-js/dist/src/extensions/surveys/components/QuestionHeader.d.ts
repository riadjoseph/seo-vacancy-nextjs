import { h } from 'preact';
import { SurveyQuestionDescriptionContentType } from '../../../posthog-surveys-types';
export declare function QuestionHeader({ question, description, descriptionContentType, forceDisableHtml, }: {
    question: string;
    description?: string | null;
    descriptionContentType?: SurveyQuestionDescriptionContentType;
    forceDisableHtml: boolean;
}): h.JSX.Element;
export declare function Cancel({ onClick }: {
    onClick: () => void;
}): h.JSX.Element;
