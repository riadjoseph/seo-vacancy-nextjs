import { CAMPAIGN_PARAMS, getCampaignParams, EVENT_TO_PERSON_PROPERTIES, getEventProperties, getReferrerInfo, } from '../utils/event-utils';
import { each, extend } from '../utils';
import { includes } from '../utils/string-utils';
export var setAllPersonProfilePropertiesAsPersonPropertiesForFlags = function (posthog) {
    var allProperties = extend({}, getEventProperties(posthog.config.mask_personal_data_properties, posthog.config.custom_personal_data_properties), getCampaignParams(posthog.config.custom_campaign_params, posthog.config.mask_personal_data_properties, posthog.config.custom_personal_data_properties), getReferrerInfo());
    var personProperties = {};
    each(allProperties, function (v, k) {
        if (includes(CAMPAIGN_PARAMS, k) || includes(EVENT_TO_PERSON_PROPERTIES, k)) {
            personProperties[k] = v;
        }
    });
    posthog.setPersonPropertiesForFlags(personProperties);
};
//# sourceMappingURL=setAllPersonProfilePropertiesAsPersonPropertiesForFlags.js.map